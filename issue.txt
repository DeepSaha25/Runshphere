In summary, the primary culprits are:

Java 21 Incompatibility: React Native 0.85 strictly requires JDK 17. Your current JDK 21 installation is corrupting the Gradle metadata and preventing compilation.

Missing Node Modules & Caches: Essential plugin files (like shared-testutil.jar) are missing from your node_modules, causing the current "Cannot read workspace metadata" errors.

C++ folly Compilation Error: The earlier C++ errors you faced were caused by the default NDK 26 using an older Clang compiler that fails to build the React Native library. I have already updated your android/build.gradle to use your installed NDK 27.1 which has the newer Clang 18 to fix this specific issue.

Please follow the 5-step remediation plan outlined at the bottom of the artifact. Let me know when you've finished those steps (specifically downgrading Java and clearing node_modules + Gradle caches), and we can try the build again!